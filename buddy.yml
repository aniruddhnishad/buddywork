- action: "Execute: apt-get update && apt-get -y install git"
    type: "BUILD"
    working_directory: "/buddy/buddywork"
    docker_image_name: "library/ubuntu"
    docker_image_tag: "latest"
    execute_commands:
    - "apt-get update && apt-get -y install git"
    setup_commands:
    - "apt-get update && apt-get -y install git"
    volume_mappings:
    - "/:/buddy/buddywork"
    cache_base_image: true
    shell: "BASH"
